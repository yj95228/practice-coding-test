# https://programmers.co.kr/learn/courses/30/lessons/42748

def solution(array, commands):
    answer = []
    for command in commands:
        i, j, k = command
        
        a = array[i-1:j]
        b = sorted(a)
        c = list(b)[k-1]
        
        answer.append(list(sorted(array[i-1:j])
    return answer
