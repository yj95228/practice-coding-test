// https://school.programmers.co.kr/learn/courses/30/lessons/12944?language=javascript
function solution(arr) {
    let sum = arr.reduce((arr,cur) => { return arr + cur; });
    return sum/arr.length;
}
