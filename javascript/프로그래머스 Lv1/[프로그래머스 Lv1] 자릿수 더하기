// https://school.programmers.co.kr/learn/courses/30/lessons/12931?language=javascript
function solution(n) {
    let numArray = String(n).split('');
    let answer = numArray.reduce((acc, cur) => {
        return Number(acc) + Number(cur)
    })
    return Number(answer)
}
